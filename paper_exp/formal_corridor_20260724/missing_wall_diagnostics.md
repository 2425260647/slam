# corridor_repeat_01 缺墙数据链检查

## 检查边界

- 使用 Clean Baseline 最终 PBStream 导出的 2600 个优化轨迹节点，只统计与节点时间差不超过 20 ms、实际通过运动过滤并写入子图的点云帧。
- 原始数据为 `/velodyne_points`，静态外参采用修正后的 `base_link -> laser_link z=0.33521 m`、yaw=-1.5708 rad；二维分析只涉及 x/y，因此 z 平移不改变墙面平面坐标。
- 模拟当前 `pointcloud_to_laserscan` 参数：高度 `[-0.515,1.435] m`、距离 `[0.3,30] m`、角分辨率 `0.00436 rad`，每个角度格保留最近点。

## 定量结果

| 区域 | 帧数 | 原始墙点 | 高度过滤后 | 二维投影端点 | 去程占据数 | 正常往返占据数 | 关闭自由空间占据数 | 回程后保留率 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| gap_near_branch | 1290 | 963378 | 963165 | 63998 | 103 | 1 | 614 | 0.97% |
| gap_far_end | 514 | 1241012 | 1238661 | 133217 | 122 | 6 | 940 | 4.92% |

### gap_near_branch

- 对应数据时间覆盖：bag 起点后 `57.71`~`520.91 s`。
- 当前高度带保留缺口原始墙点比例：`99.98%`。
- 高度过滤后点成为二维最近端点的比例：`6.64%`。
- 缺口原始墙点 z 范围：`-0.5642937421798706`~`0.47488752007484436 m`，中位数 `-0.025043506175279617 m`。
- 地图缺口框：占据 `1`、空闲 `1072`、未知 `656` 个栅格。

- 去程同区域占据 `103` 个栅格，最终仅保留 `0.97%`。

- 完整往返但关闭自由空间插入后，占据栅格恢复为 `614` 个，是正常完整往返结果的 `614.0` 倍；该区域空闲栅格从 `1072` 降为 `0`。

- 去回程优化轨迹横向分离中位数：`0.475 m`，P90：`0.585 m`。
- 去程/回程墙端点拟合线在区域中点的 y 差：`0.008 m`；去程点带 P90-P10 中位宽度 `0.180 m`，回程为 `0.036 m`。

### gap_far_end

- 对应数据时间覆盖：bag 起点后 `245.70`~`342.01 s`。
- 当前高度带保留缺口原始墙点比例：`99.81%`。
- 高度过滤后点成为二维最近端点的比例：`10.75%`。
- 缺口原始墙点 z 范围：`-0.6861667633056641`~`0.5583638548851013 m`，中位数 `-0.05742298811674118 m`。
- 地图缺口框：占据 `6`、空闲 `2146`、未知 `1229` 个栅格。

- 去程同区域占据 `122` 个栅格，最终仅保留 `4.92%`。

- 完整往返但关闭自由空间插入后，占据栅格恢复为 `940` 个，是正常完整往返结果的 `156.7` 倍；该区域空闲栅格从 `2146` 降为 `0`。

- 去回程优化轨迹横向分离中位数：`0.070 m`，P90：`0.269 m`。
- 去程/回程墙端点拟合线在区域中点的 y 差：`0.004 m`；去程点带 P90-P10 中位宽度 `0.184 m`，回程为 `0.044 m`。

## 判读说明

- 若原始墙点接近 0，则缺墙源于采集回波或区域定位。
- 若原始墙点充足但高度过滤后接近 0，则源于高度带。
- 若高度过滤后充足但二维端点接近 0，则源于同角度最近点竞争。
- 若二维端点仍充足但最终地图无占据，则应继续检查轨迹配准、子图概率融合和自由空间射线。

## 阶段结论

两处墙体在只含去程的地图中均已建立，但在正常完整往返后大部分占据栅格消失；关闭自由空间插入后，同一完整往返数据中的墙体重新出现。原始回波、高度过滤和二维投影均有充分墙点，因此缺墙不是录包、安装高度或投影过滤造成，而是回程有限回波射线在小量局部位姿/子图错位下反复穿过早期墙栅格，将其更新为空闲，并由后生成子图纹理覆盖早期占据纹理。完全关闭自由空间会失去可通行区域证据，只用于确认因果，不应作为最终建图参数。
