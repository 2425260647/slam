# 实时建图与定位需要拷贝的文件清单

## 一、必须拷贝的核心文件

### 1. 配置文件（最重要）
```bash
# 优化版配置（推荐使用）
src/my_navigation/config/cartographer_scout_2d.lua

# Baseline配置（作为对比）
test_results/configs/cartographer_scout_2d_baseline_20260826.lua
```

### 2. Launch启动文件
```bash
# 实时建图launch（包含健康监控）
src/my_navigation/launch/real_scout_mapping_local_grid.launch
```

### 3. 包清单文件
```bash
# 确保依赖正确
src/my_navigation/package.xml
src/my_navigation/CMakeLists.txt
```

---

## 二、可选文档（用于参考和调试）

### 1. 测试报告（了解参数效果）
```bash
test_results/reports/对比测试报告_Baseline_vs_Optimized_20260826.md
test_results/reports/优化参数对比_20260826.md
```

### 2. 配置说明
```bash
# 如果需要现场调参
test_results/reports/分析报告_Cartographer实车部署问题与优化方案.md
```

### 3. 监控脚本（用于测试）
```bash
test_results/scripts/monitor_test.sh
```

---

## 三、实车部署步骤

### 步骤1: 拷贝核心文件到实车
```bash
# 在虚拟机上打包
cd /home/slam/slam_ws
tar -czf cartographer_optimized_deploy.tar.gz \
    src/my_navigation/config/cartographer_scout_2d.lua \
    src/my_navigation/launch/real_scout_mapping_local_grid.launch \
    test_results/configs/cartographer_scout_2d_baseline_20260826.lua

# 传输到实车（假设实车IP为192.168.1.100）
scp cartographer_optimized_deploy.tar.gz slam@192.168.1.100:~/
```

### 步骤2: 实车上解压部署
```bash
# SSH登录实车
ssh slam@192.168.1.100

# 解压文件
cd ~
tar -xzf cartographer_optimized_deploy.tar.gz

# 拷贝到工作空间
cd ~/catkin_ws  # 或实车的工作空间路径
cp ~/src/my_navigation/config/cartographer_scout_2d.lua src/my_navigation/config/
cp ~/src/my_navigation/launch/real_scout_mapping_local_grid.launch src/my_navigation/launch/

# 编译
catkin_make_isolated --install --use-ninja --pkg my_navigation
```

### 步骤3: 启动测试
```bash
# 启动Cartographer实时建图
source install_isolated/setup.bash
roslaunch my_navigation real_scout_mapping_local_grid.launch \
    cloud_topic:=/velodyne_points \
    odom_topic:=/odom \
    target_frame:=laser_link \
    rviz:=true \
    enable_monitor:=true
```

---

## 四、关键配置差异（虚拟机 vs 实车）

### 虚拟机（Ubuntu 20.04 Noetic）
- ROS版本: Noetic
- Python: 3.8
- 测试环境，使用rosbag仿真

### 实车（Ubuntu 18.04 Melodic）
- ROS版本: Melodic
- Python: 2.7（可能）
- 实时建图，硬件传感器

### 需要注意的兼容性问题
1. ✅ **lua配置文件**：完全兼容，无需修改
2. ✅ **launch文件**：兼容，topic名称可能需要调整
3. ⚠️ **Python脚本**（如有）：需要检查Python2/3兼容性
4. ⚠️ **TF frame名称**：实车可能使用不同的frame名称（如`/base_link` vs `base_link`）

---

## 五、快速拷贝命令（推荐）

### 最小化部署（只拷贝必需文件）
```bash
# 创建部署包
cd /home/slam/slam_ws
mkdir -p deploy_package
cp src/my_navigation/config/cartographer_scout_2d.lua deploy_package/
cp src/my_navigation/launch/real_scout_mapping_local_grid.launch deploy_package/
cp test_results/configs/cartographer_scout_2d_baseline_20260826.lua deploy_package/baseline_config.lua
tar -czf cartographer_deploy_minimal.tar.gz deploy_package/

echo "部署包已创建: cartographer_deploy_minimal.tar.gz"
```

### 完整部署（包含文档）
```bash
# 创建完整部署包
cd /home/slam/slam_ws
mkdir -p deploy_package_full/config
mkdir -p deploy_package_full/launch
mkdir -p deploy_package_full/docs
mkdir -p deploy_package_full/scripts

cp src/my_navigation/config/cartographer_scout_2d.lua deploy_package_full/config/
cp test_results/configs/cartographer_scout_2d_baseline_20260826.lua deploy_package_full/config/
cp src/my_navigation/launch/real_scout_mapping_local_grid.launch deploy_package_full/launch/
cp test_results/reports/对比测试报告_Baseline_vs_Optimized_20260826.md deploy_package_full/docs/
cp test_results/reports/优化参数对比_20260826.md deploy_package_full/docs/
cp test_results/scripts/monitor_test.sh deploy_package_full/scripts/

tar -czf cartographer_deploy_full.tar.gz deploy_package_full/

echo "完整部署包已创建: cartographer_deploy_full.tar.gz"
```

---

## 六、总结

**必须拷贝（3个文件）**：
1. `cartographer_scout_2d.lua` - 优化版配置
2. `real_scout_mapping_local_grid.launch` - 启动文件
3. `cartographer_scout_2d_baseline_20260826.lua` - baseline配置（对比用）

**建议拷贝（2-3个文档）**：
1. `对比测试报告_Baseline_vs_Optimized_20260826.md` - 了解优化效果
2. `优化参数对比_20260826.md` - 参数调整参考

**可选拷贝**：
- 监控脚本、分析报告等（用于调试）

**部署方式**：
- 使用上述打包命令创建tar.gz
- 通过scp传输到实车
- 实车上解压并编译部署
